# client

React 19 + Vite + Tailwind CSS v4 UI for Mellith.

```sh
bun run dev
```

Uses the typed Hono client from `@mellith/server/client` and types from `@mellith/types`. See the [root README](../README.md) for workspace commands.
